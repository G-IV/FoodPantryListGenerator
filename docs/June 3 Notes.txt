I'm going to be writing up a bunch of notes during a meeting with Tina.

People are holding 1 of 2 cards
 - card a, is a large card that is easily seen that has household size details.  People shouldn't be able to get through this station without this household card.

On the duplicates:
Duplicate - matches previous scan (record n == record n-1):  Need a reassurance message that nothing went wrong, you can continue processing the customer.  "Duplicate, proceed to next customer". Any color I want.
Duplicate - does not match previous (record n != record n-1): Ignore, but leave code in place to be easily enabled later.
Flagged - InvNmbrs.csv match: "This barcode has been flagged, please ask a cart guide to escort customer to Oasis administrator"

We can remove the name & number from the top line 

===================
In terminal, replace "source code: url" with "recent release: url"

===================
When InvNmbrs.csv is autocreated, the reported location for the file in the termanil is incorrect, while the actual location is correct.
Ideal behavior: The file is created in the same place the exe exists, and is reported properly.

Remove this feature: instead, if it isn't there, don't create it.  Instead, we can just check for it periodically to see if it is there.

===================
After combining the oasis file with the scanned file
what is looked for: 
 - Fine: is there a single number with all the relavent details (means only scanned at oasis)
 - Not fine: a single number with no relavent details (mean they were scaned here, but not at oasis) -> needs review

Currently, the admin copies the barcodes from this scanning tool to the exported oasis file, then copies the dates on a second pass.
We'd like to have something indicating the numbers that need to be reviewed by the administrator.  

We have a few options:
 - a tool that can look at both files and report.  Excel?  Web App?

===================
I am free to try making a web app for this as well.


